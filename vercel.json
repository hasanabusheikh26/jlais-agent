{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/token", "dest": "/server.js" },
    { "src": "/session", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
